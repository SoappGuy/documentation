# Enabling vale linter in `neovim`

You need .vale.ini file in your home folder. (You can generate one at https://vale.sh/generator)
Then run 
```sh
~/.local/share/nvim/mason/packages/vale/vale sync
```
